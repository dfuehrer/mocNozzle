@echo this is supposed to install the packages with pip
@echo mocNozzle.py requires:
@echo numpy pandas scipy matplotlib 
@echo Note: if you are not an Admin, then use pip with the --user flag for a local install
python -m pip install numpy pandas scipy matplotlib 
